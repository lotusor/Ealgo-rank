# 直接依赖。传递依赖由 pip 自行解析，不锁死以便安全更新。
Django==5.2.17
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
django-filter==26.1
drf-spectacular==0.30.0
celery==5.6.3
django-celery-beat==2.9.0
redis==8.1.0
python-dotenv==1.2.2
requests==2.34.2
Pillow==12.3.0

# 生产环境额外需要（Docker 部署已启用）
psycopg[binary]==3.2.*
gunicorn==23.*
# 生产静态资源（admin / drf-spectacular 等）由后端经 /static/ 提供，避免额外挂共享卷
whitenoise==6.9.0

# 统一认证中心 JWT 验签 SDK（RS256 + JWKS，离线验签）
# 注意：该 SDK 未发布到 PyPI（演示项目）。Docker 构建时由 docker/Dockerfile
# 从 vendored 源码（./vendor/lotus-passport-sdk）或 GitHub 源码安装，不在此处声明。
# 开发期本地安装：pip install -e ../lotus-passport-sdk[drf]
