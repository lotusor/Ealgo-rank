# E-algo Rank 项目接手文档（当前状态 · 2026-08-21）

> 本文是「当前真实状态」接手文档，聚焦 7 个维度。更细的 API 表 / 配置全表 / 历史方案见同目录 `HANDOFF.md`（2026-08-16 版，仍保留作详细参考）。
> **重要前提**：本仓库大量改动尚未 `git commit`（见 §6），部署服务器上的运行版本领先于 git，接手第一件事是先理清并提交这些改动。

---

## 1. 项目背景、技术栈与整体架构

**背景**：eacm.cn 旗下「算法竞赛积分排名平台」。用户关联 AtCoder / Codeforces / 牛客等平台账号后，系统定时爬取各平台榜单，按规则计算积分并生成排名。

**技术栈**
- 前端：Vue 3 + Vite + TypeScript + Pinia + Vue Router + Tailwind CSS
- 后端：Django 5 + DRF + djangorestframework-simplejwt + Celery / Celery Beat
- 存储：开发期 SQLite；生产计划 PostgreSQL（复用服务器宝塔内置实例）+ Redis（宝塔内置，独立 db 索引）
- 统一登录：**Lotus Passport**（RS256 / JWKS 离线验签，护照方 `passport.eacm.cn`）；本地 HS256 simplejwt 仅作 root 紧急兜底
- 部署：Docker Compose（backend / worker / beat / rank-nginx）+ 护照容器 nginx 薄转发 shim

**整体架构**
- 前端 SPA（构建产物 `dist`）与后端 API **同域** `rank.eacm.cn`；Nginx 把 `/api` `/static` `/media` 反代到 `backend:8001`，其余走 SPA fallback。
- 登录统一走护照：前端 `fetch /api/v1/oauth/{p}/login/` 取 `authorize_url` → 跳转第三方 → 护照完成登录后 **302 回 `rank.eacm.cn/auth/callback` 并把 JWT 放在 URL fragment**（`#access_token=…&refresh_token=…&passport_user_id=…`）→ 前端读 `window.location.hash` 建会话。
- 第三方平台数据由 `crawlers/` 脚本 + Celery 定时任务抓取入库，积分引擎消费计算排名。

---

## 2. 当前代码结构与核心模块职责

```
e-algo-rank/
├── backend/                 Django 项目
│   ├── config/settings/     base.py（含 PASSPORT_* 配置）、urls、asgi/wsgi、celery
│   └── apps/
│       ├── accounts/        认证、用户解析、安全戳；auth.py 见下
│       ├── schools/         学校与认领申请
│       ├── contests/        比赛模型与抓取登记
│       ├── crawler/         爬取任务调度
│       ├── ranking/         积分引擎
│       ├── common/          通用工具
│       └── announcements/   系统公告
├── frontend/                Vue 项目（src/ 下）
│   ├── api/index.ts         startPassportOAuth(provider) 取 authorize_url 跳转；axios client
│   ├── components/auth/PassportLoginButtons.vue   GitHub/QQ 选择按钮（微信隐藏）
│   ├── views/auth/AuthCallbackView.vue            读 hash fragment 解析 token
│   ├── views/LoginView.vue、RegisterEntryView.vue 集成 passport 登录入口
│   └── .env.production      VITE_PASSPORT_URL / VITE_PASSPORT_LOGIN_URL（生产覆盖）
├── crawlers/                atcoder / cf / nowcoder scrape + 校验脚本
├── deploy/                  rank-nginx.conf（rank-nginx:8003）、rank-proxy.conf（护照 nginx 443 薄转发）
├── docker/                  Dockerfile（python:3.13-slim，含 backend+crawlers，gunicorn）
└── docker-compose.yml       backend / worker / beat / rank-nginx（均 host 网络）
```

**核心认证模块**（`backend/apps/accounts/auth.py`）
- `AlgoRankPassportAuthentication`：继承护照 SDK 的 `PassportAuthentication`，**RS256 优先**——先读 JWT 头 `alg`，仅 RS256 交给父类走 JWKS 离线验签；本地 HS256 令牌返回 `None` 放行给 simplejwt（root 兜底）。算法混淆攻击（用公钥当 HMAC 密钥）仍被拒。
- `resolve_passport_user(identity)`：按 `passport_user_id`(UUID) 关联/首建本地 `User`；`username` 用该 UUID 占位（因 `AbstractUser.username` 必填唯一），`set_unusable_password()`（无本地密码，密码登录仅 root 兜底）。
- `StampedJWTAuthentication`：在 simplejwt 基础上比对 `security_stamp`，改密/解锁/登出全设备时轮换戳，旧令牌下次请求即 401。

---

## 3. 已完成的任务及进度百分比

| 模块 | 状态 | 进度 | 说明 |
|---|---|---|---|
| Django 骨架 + 7 个 app | ✅ | 100% | 自定义用户模型 `accounts.User` |
| 认证与用户 API | ✅ | 100% | 注册/登录/JWT/改密/平台账号/用户名认领 |
| 登录安全 | ✅ | 100% | 失败锁定+自动/管理员解锁；多维限流（账号/设备指纹/IP 兜底）；撞库与会话探测检测；安全戳吊销；**校园网 NAT 友好** |
| Lotus Passport 接入契约 | ✅ | 100% | 2026-08-16 实测核实：iss=`lotus-passport`、RS256、不签发 aud、access 30min/refresh 14d、redirect_uri 白名单 origin 级含 `rank.eacm.cn`、JWKS 公网可达 |
| 通行证登录 UI 闭环 | ✅(代码) | 90% | 按钮组件 + `startPassportOAuth` + fragment 回调解析 **代码已完成，但未 commit 且未真人 E2E 走通** |
| 积分引擎 / 排名 | ✅ | 100% | |
| 爬虫（三平台）+ 定时自动爬取 + 去重 | ✅ | 100% | `CrawlConfig` 单例 + Beat 每日调度；任务级 1h 去重 + 比赛级幂等 |
| 公告 / 站内信 | ✅ | 100% | |
| 管理员申请 / 学校认领 | ✅ | 100% | |
| 安全中心页 `SecurityView` | 🆕 | 90% | 新建未提交 |
| 生产化部署配置 | ⏳ | 70% | Docker / Nginx / PG / Redis 配置就绪，未提交、未验证完整链路 |

**整体项目进度约 85%**（产品功能基本闭环，统一登录代码就绪，生产部署与真人联调收尾中）。

---

## 4. 进行中与待处理的任务清单及优先级

**P0（阻塞/高风险，先处理）**
- **提交当前未提交的工作树**：含大量登录/部署相关改动（见 §6），服务器若从 git 重建会丢失。先 `git status` 盘点、分次提交。
- **真人浏览器走通 GitHub/QQ 登录 → 护照确认页 → 回跳 rank 完成登录**：需护照侧第三方交换正常（当前护照→GitHub 出站偶发 504，见 §5）。

**P1**
- 生产部署执行与验证：`docker compose up -d --build`；核对 rank-nginx SPA fallback、`/api` 反代、CORS、`/auth/callback` 路由。
- 爬取预筛逻辑验收：只爬「有已关联平台 ID 用户参与」的比赛，省流量与存储。

**P2**
- 「我的排名」入口（个人成绩页展示榜单名次）。
- 微信登录（等护照侧启用微信 provider）。
- 决策：dev / prod 双套 GitHub OAuth App（受单 callback URL 限制）。

---

## 5. 已知问题、技术债务与风险点

- **未提交改动风险（最高）**：大量未 commit 的登录/部署改动，服务器若从 git 重建即丢失。见 §6 清单。
- **JWT 不签发 `aud`**：防御盲区（护照侧统一决策，rank 仅消费；离线验签不校验 audience）。
- **离线验签的吊销边界**：用户主动登出只进护照 Redis 黑名单；rank 离线验签下旧 access token 需自然过期（≤30min）才失效。
- **Vite 构建陷阱**：不 `--mode production` 会把 `.env` 本地开发值烤进生产构建（已用 `.env.production` 覆盖规避，但 CI/手动构建务必带该参数）。
- **Docker bind mount inode 陷阱**：`rm -rf dist` 重建后容器内挂载指向旧 inode，须 `docker restart rank-rank-nginx-1` 重新解析。
- **GitHub OAuth 单 callback 限制**：dev / prod 二选一，需决策双 App 还是沿用 dev mock。
- **护照→GitHub 出站 504**：护照侧设施/网络问题，影响真·第三方交换（属护照仓库范畴，rank 侧无法独立修复）。
- **微信 OAuth 护照侧未启用**：护照 `WECHAT_CLIENT_ID/SECRET` 留空，rank 前端已隐藏微信入口。
- **学校 seed 迁移未提交**：`backend/apps/schools/migrations/0005_seed_schools.py` 为未跟踪文件。

---

## 6. 环境配置、部署流程与关键账号信息

**域名**：`rank.eacm.cn`（前端 + API 同域）。
**护照配置**（backend `config/settings/base.py`）：
- `PASSPORT_BASE_URL=https://passport.eacm.cn`
- `PASSPORT_ISSUER=lotus-passport`
- `PASSPORT_JWKS_CACHE_TTL=600`、`PASSPORT_JWKS_LEEWAY=10`
- `USER_RESOLVER=apps.accounts.auth.resolve_passport_user`

**前端生产环境变量**（`frontend/.env.production`）：
- `VITE_PASSPORT_URL=https://passport.eacm.cn`
- `VITE_PASSPORT_LOGIN_URL=https://account.eacm.cn`

**部署拓扑**
- 复用服务器宝塔内置 PostgreSQL / Redis（Redis 用独立 db 索引，不动护照数据）。
- `docker-compose.yml`：backend / worker / beat 三个服务跑业务逻辑，`rank-nginx`（nginx:1.27-alpine）承载 SPA + 反代，均 `network_mode: host`。
- `deploy/rank-nginx.conf`：纯 HTTP 监听 `:8003`，root=前端 dist，反代 `127.0.0.1:8001`。
- `deploy/rank-proxy.conf`：护照容器内 nginx 443 薄转发到 `127.0.0.1:8003`，TLS 用 `*.eacm.cn` 通配证，带 `X-Forwarded-Proto`。
- 媒体文件宿主机 bind mount `./volumes/media` 持久化。

**部署命令（服务器上）**
```bash
cd <repo>/e-algo-rank
docker compose up -d --build          # 后端/worker/beat/rank-nginx
# 前端构建产物拷到宝塔网站根目录
npm --prefix frontend run build -- --mode production
cp -r frontend/dist /www/wwwroot/rank.eacm.cn/dist
docker restart rank-rank-nginx-1      # bind mount 重建后必须重启
```

**当前未提交改动清单（接手务必先处理）**
- 新增（未跟踪）：`frontend/src/components/auth/PassportLoginButtons.vue`、`frontend/.env.production`、`frontend/src/views/user/SecurityView.vue`、`backend/apps/schools/migrations/0005_seed_schools.py`、`deploy/rank-nginx.conf`、`deploy/rank-proxy.conf`（替代旧 `deploy/nginx-rank.eacm.cn.conf`）、`frontend/build-out/`
- 修改（已跟踪）：`docker-compose.yml`、`docker/Dockerfile`、`frontend/src/api/index.ts`、`frontend/src/api/types.ts`、`frontend/src/components/layout/NavBar.vue`、`frontend/src/router/index.ts`、`frontend/src/views/LoginView.vue`、`frontend/src/views/auth/AuthCallbackView.vue`、`frontend/src/views/auth/RegisterEntryView.vue`、`frontend/index.html`、`backend/apps/accounts/serializers.py`、`backend/apps/accounts/validators.py`、`backend/config/settings/base.py`、`requirements.txt`
- 删除：`deploy/nginx-rank.eacm.cn.conf`

**关键账号 / 凭证**
- 宝塔面板（管理 PG / Redis / Nginx / 网站根目录）。
- 服务器 root：聊天记录中曾暴露密码，**部署后务必 `passwd root` 更换**。
- GitHub OAuth App（Client ID / Secret）：dev 与 prod 受单 callback 限制。
- RS256 / 通配证书由护照侧统一保管，rank 不持私钥。

---

## 7. 相关文档与联系人

- **本仓库 `HANDOFF.md`**（2026-08-16）：完整 API 表、配置全表、生产化/联调方案草案，接手详细参考首选。
- **护照仓库 `docs/integration/project1-ealgo-rank.md`**：统一登录接入契约（护照方维护，rank 只消费契约）。
- `overview.md`：顶层概览。
- **联系人**：项目 owner = JXGM；护照对接 = lotus-passport 维护方（同一团队）。
- **关键外部依赖**：GitHub OAuth（dev/prod App）、宝塔面板、eacm.cn 域名/DNS/通配证书。
