# 前端环境变量示例（复制为 .env.production 或在构建时内联传入）
# 统一认证中心（lotus-passport）后端 API 地址；前端登录/刷新令牌均打到这里
# 2026-08-16 已确认：passport 后端 = https://passport.eacm.cn（公网 HTTPS，JWKS 亦走此域名）
VITE_PASSPORT_URL=https://passport.eacm.cn

# 本地 Django 后端地址（仅 dev 代理用）；
# 生产前端经同域 Nginx 反代 /api/v1，baseURL 用相对路径，无需此项
VITE_API_TARGET=https://rank.eacm.cn

# 说明：
# - 生产构建：VITE_PASSPORT_URL=https://passport.eacm.cn npm run build --emptyOutDir false
# - 浏览器直连 passport 的 /api/v1/oauth/github/login/ 与 /api/v1/token/refresh/ 已由
#   passport 侧 CORS 放行 rank.eacm.cn（origin 级）
# - 回跳地址固定为 https://rank.eacm.cn/auth/callback（passport redirect_uri 白名单已含该 origin）
